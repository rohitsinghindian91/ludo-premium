name: vludo_premium
description: Ludo Premium with Firebase
publish_to: 'none'
version: 1.0.0+23
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.12.0
  cloud_firestore: ^4.15.0
  url_launcher: ^6.2.2
flutter:
  uses-material-design: true